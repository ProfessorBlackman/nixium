/**
 * Reclaim. Tasks 1.7–1.10, Phase 1.
 */
export default function Reclaim() {
  return (
    <section className="stack">
      <div className="card">
        <h2>Not built yet</h2>
        <p>
          Reclaiming arrives in milestone M3, behind the full preview → confirm → execute → report
          pipeline.
        </p>
        <p className="muted">
          The pipeline is built against trash first, before any category worth reclaiming exists —
          so there is never pressure to bypass it “just this once”.
        </p>
      </div>
    </section>
  );
}
