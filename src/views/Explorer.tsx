/**
 * Space explorer. Task 1.2–1.6, Phase 1 — the milestone that makes nix worth installing.
 */
export default function Explorer() {
  return (
    <section className="stack">
      <div className="card">
        <h2>Not built yet</h2>
        <p>
          The space explorer is milestone M2: a treemap and tree table over the space model,
          streaming and cancellable, answering “where did my disk go?”.
        </p>
        <p className="muted">
          It needs no privileged helper for paths you already own, which is why it can ship before
          the helper is finished.
        </p>
      </div>
    </section>
  );
}
